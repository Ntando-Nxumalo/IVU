package com.ntando.ivu.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Primary accent warm orange color (`#E88A68`) used across app bars, primary action buttons, FABs, and selection highlights.
 */
val PrimaryOrange = Color(0xFFE88A68)

/**
 * Dark warm brown text color (`#3D2B1F`) used for primary headings, body text, and contrast elements on light backgrounds.
 */
val DarkBrown = Color(0xFF3D2B1F)

/**
 * Light warm cream background color (`#FFF8F0`) used as default background container color in light theme.
 */
val BackgroundLight = Color(0xFFFFF8F0)

/**
 * Dark theme surface/background color (`#121212`) complying with Material Design dark surface guidelines.
 */
val BackgroundDark = Color(0xFF121212)

/**
 * Material 3 default dark theme primary palette color.
 */
val Purple80 = Color(0xFFD0BCFF)

/**
 * Material 3 default dark theme secondary palette color.
 */
val PurpleGrey80 = Color(0xFFCCC2DC)

/**
 * Material 3 default dark theme tertiary palette color.
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * Material 3 default light theme primary palette color.
 */
val Purple40 = Color(0xFF6650a4)

/**
 * Material 3 default light theme secondary palette color.
 */
val PurpleGrey40 = Color(0xFF625b71)

/**
 * Material 3 default light theme tertiary palette color.
 */
val Pink40 = Color(0xFF7D5260)
